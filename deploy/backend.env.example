# Backend runtime config (PROD on server)
PORT=3001
NODE_ENV=production

# Postgres in docker-compose (service name "db")
PGHOST=db
PGPORT=5432
PGDATABASE=yardena_test
PGUSER=yardena
PGPASSWORD=yardena_pass

# CORS / Frontend
FRONTEND_URL=https://linuxtest3.woki.co.il

# Sessions
SESSION_SECRET=CHANGE_ME_LONG_RANDOM_STRING

# Optional: Google login (if used in your backend)
GOOGLE_CLIENT_ID=
