# Local/dev example (do NOT commit real secrets)
PORT=3001
NODE_ENV=development

# Postgres (for docker-compose prod PGHOST should be "db")
PGHOST=localhost
PGPORT=5432
PGDATABASE=yardena_test
PGUSER=yardena
PGPASSWORD=yardena_pass

# CORS (optional)
FRONTEND_URL=http://localhost:3000

# Sessions
SESSION_SECRET=CHANGE_ME_LONG_RANDOM_STRING

# Optional: Google login
GOOGLE_CLIENT_ID=
